# 上班陪伴 Web 像素风 UI 规格稿

## 1. 风格结论

当前产品视觉方向正式建议为：

`现代 Web UI + 像素风牛马角色主视觉 + 像素化局部反馈`

这意味着：

1. 页面布局、表单、列表、信息层级遵循现代网页产品习惯
2. 角色、状态反馈、局部装饰和关键动效使用像素风
3. 不做全站纯像素 UI，避免影响可读性和操作效率

## 2. 视觉原则

### 2.1 保持现代 Web 的部分

这些区域建议保持现代网页风格：

1. 登录表单
2. 群组列表和成员列表
3. 设置页表单
4. 打卡记录表格
5. 顶部导航结构

原因：

1. 保证易用性
2. 方便后续扩展
3. 不会因为全像素化导致信息密度变差

### 2.2 使用像素风的部分

这些区域建议明显像素化：

1. 牛马角色本体
2. 首页 Hero 主场景
3. 心情状态图标
4. 打卡成功反馈
5. 倒计时状态贴纸
6. 外卖转盘动画
7. 空状态插图

## 3. 整体气质

关键词建议：

1. 像素陪伴
2. 打工人轻游戏感
3. 温暖
4. 轻松
5. 苦中作乐

避免：

1. 冷淡赛博风
2. 纯复古街机感
3. 过暗背景
4. 过强故障特效

这个产品应该更像“陪你上班的小网页游戏界面”，不是“硬核复古游戏菜单”。

## 4. 首页视觉方案

### 4.1 首页定位

首页是产品灵魂页，核心体验是：

1. 打开页面就看到像素牛马在上班
2. 很快知道自己今天的状态
3. 一眼看到离下班还有多久
4. 可以顺手打卡和选心情

### 4.2 页面结构

```text
顶部导航
  ↓
首页 Hero 主视觉
  左：像素牛马上班场景
  右：今日状态 / 倒计时 / 打卡
  ↓
下方功能区
  心情选择 / 群组入口 / 最近外卖结果 / 今日摘要
```

### 4.3 Hero 场景建议

Hero 推荐做成一个固定像素场景：

1. 牛马坐在工位前
2. 面前是电脑
3. 桌上有咖啡、便签、午饭元素
4. 背后可以有窗户、时钟、文件柜等简化元素

场景要求：

1. 明显是像素风
2. 但构图干净，不要信息噪音太多
3. 像素角色是主角，家具只是辅助

### 4.4 Hero 状态变化

首页 Hero 需要跟用户状态联动：

1. 未打卡：牛马刚坐下，状态偏“准备开工”
2. 已上班：牛马进入工作状态
3. 快下班：牛马表情更期待
4. 已下班：牛马明显放松

### 4.5 今日心情联动

建议心情和视觉变化这样绑定：

1. 干劲满满：眼神发亮，桌面更整齐，状态更精神
2. 普通营业：平稳表情
3. 累了：眼皮半垂，桌边咖啡更突出
4. 想吃点好的：表情更馋，桌边出现午饭相关小元素
5. 快下班吧：角色有明显期待感

## 5. 群组页视觉方案

### 5.1 页面定位

群组页不是首页那种沉浸页，而是“带像素气质的实用协作页”。

### 5.2 风格建议

群组页整体仍以现代排版为主，但加这些像素元素：

1. 群组头像或群组徽章像素化
2. 外卖分类图标像素化
3. 转盘结果反馈像素化
4. 空状态插图像素化

### 5.3 转盘区域

这里是群组页最适合做像素强化的地方：

1. 转盘主体可采用像素边框和像素色块
2. 结果出现时做像素闪烁或像素彩带
3. 最终结果名可以用更游戏化的展示方式

但要注意：

1. 不要让转盘本身难读
2. 候选项文字仍然保持现代字体展示

## 6. 组件风格落点

### 6.1 建议保持现代风格的组件

1. `AppTopNav`
2. `ProfileMenu`
3. `GroupMemberList`
4. `AttendanceTable`
5. `SettingsForm`

这些组件以现代 UI 为主，只需在边角和色彩上统一整体气质。

### 6.2 建议像素化的组件

1. `CompanionHero`
2. `MoodPicker`
3. `ClockStatusBadge`
4. `SpinWheelPanel`
5. `EmptyStateBlock`
6. `ResultToast`

### 6.3 半像素化组件

这类组件建议采用现代结构 + 像素装饰：

1. `StatusCountdownCard`
2. `GroupQuickCard`
3. `AttendanceSummaryCard`

做法建议：

1. 卡片主体仍现代
2. 标题角标、状态角标、按钮反馈可像素化

## 7. 色彩方向

### 7.1 主配色建议

推荐使用偏暖、偏游戏感但不过分高饱和的颜色：

1. 主色：砖红 / 柿子橙
2. 辅色：像素米黄 / 鼠尾草绿 / 灰蓝
3. 深色：炭灰 / 暖黑
4. 背景：米白 / 浅奶油色 / 很淡的暖灰

### 7.2 颜色使用原则

1. Hero 场景颜色更丰富
2. 功能卡片颜色更克制
3. 状态色要清楚，但不要刺眼
4. 不做整页黑底霓虹像素风

## 8. 字体建议

### 8.1 正文字体

正文仍建议使用现代中文无衬线字体：

1. `Noto Sans SC`
2. `PingFang SC`

原因：

1. 中文像素字体在正文和表单中可读性通常不够稳定
2. 长时间使用更容易累

### 8.2 像素字体使用范围

像素字体只建议用于小范围强调：

1. Hero 小标题
2. 倒计时标签
3. 转盘结果标题
4. 状态贴纸

不要用于：

1. 长段正文
2. 表格内容
3. 表单标签大面积展示

## 9. 按钮与边框建议

### 9.1 按钮

按钮建议分两类：

1. 主功能按钮：现代按钮外形，点击时有像素反馈
2. 场景按钮：可做成更明显的像素按钮样式

应用建议：

1. `上班打卡` `下班打卡` 用现代主按钮
2. `开始转盘` 可以更像素、更有游戏感

### 9.2 边框

1. 普通信息卡片不必强做锯齿像素边框
2. 重点模块可用像素边角或像素描边装饰
3. 像素边框只用于强调，不要全站滥用

## 10. 动效建议

第一版动效建议集中在最有价值的地方：

1. 牛马角色切换心情时表情变化
2. 打卡成功后出现像素小贴纸或小闪光
3. 转盘启动时有像素滚动感
4. 结果揭晓时有短暂像素庆祝反馈

不建议：

1. 全站做持续闪烁
2. 大面积像素噪点背景
3. 频繁抖动动画

## 11. 首页组件树建议

```text
HomePage
  AppTopNav
  HomeHeroSection
    CompanionHero
    StatusCountdownCard
    ClockActionPanel
  HomeDashboardGrid
    MoodPicker
    GroupQuickCard
    RecentSpinCard
    AttendanceSummaryCard
```

其中：

1. `CompanionHero` 是像素主视觉核心
2. `StatusCountdownCard` 是功能核心
3. `MoodPicker` 是情绪交互核心

## 12. 群组页组件树建议

```text
GroupPage
  AppTopNav
  GroupHeader
  GroupContent
    GroupInfoPanel
    FoodOptionPanel
  SpinSection
    SpinWheelPanel
    SpinResultBanner
  SpinHistoryList
```

其中：

1. `SpinWheelPanel` 是像素风重点区
2. `FoodOptionPanel` 保持现代实用

## 13. 风格执行规则

后面不管是设计图还是前端实现，都建议守住这几条：

1. 像素风服务于角色和情绪，不要让整个产品变成复古游戏壳子
2. 实用信息永远优先清晰
3. 首页氛围最重，设置页和记录页可以更朴素
4. 群组页要兼顾效率和趣味，不要只剩花哨

## 14. 下一步建议

这份规格确认后，最适合继续补的是：

1. 首页组件字段清单
2. 群组页组件字段清单
3. API 接口清单
4. 首页视觉草图或首屏 mockup
