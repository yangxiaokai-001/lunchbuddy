# 上班陪伴 Web 本地版 MVP 技术方案

## 1. 当前阶段目标

当前第一版目标调整为：

`先做一个运行在你自己电脑上的本地 Web MVP`

这版重点是验证：

1. 你会不会愿意每天打开它
2. 首页的陪伴感是否成立
3. 打卡、工时统计、外卖转盘是否顺手
4. 像素风牛马角色是否能撑起产品记忆点

这版先不追求真正的多人在线协作，也不追求正式上线。

## 2. 为什么先做本地版

原因很直接：

1. 不花钱
2. 技术复杂度低
3. 开发和调试快
4. 产品形态还在探索期，没必要先把云端基础设施做重
5. 后面如果决定上线，再迁移数据库也来得及

## 3. 本地版整体架构

第一版建议使用：

1. 前端框架：`Next.js`
2. 语言：`TypeScript`
3. 样式：`Tailwind CSS`
4. 组件：`shadcn/ui`
5. 数据库：`SQLite`
6. ORM：`Prisma`
7. 本地运行方式：`npm run dev`

架构关系可以理解为：

```text
浏览器
  ↓
Next.js 页面
  ↓
Next.js API / Server Actions
  ↓
Prisma
  ↓
SQLite 本地数据库文件
```

## 4. 数据存储在哪里

这版数据将存在你电脑里的本地数据库文件中。

建议方式：

1. 在项目目录内放一个 SQLite 数据库文件
2. 比如文件名为 `prisma/dev.db`
3. 通过 Prisma 访问这个文件

这意味着：

1. 数据不会上传到云端
2. 你关掉项目，数据仍然保留在本地文件里
3. 你换电脑时，需要把项目和数据库文件一起带走

## 5. 这版适合做什么

适合：

1. 自己每天使用
2. 验证首页体验
3. 验证打卡与工时统计逻辑
4. 验证外卖长期候选池和转盘流程
5. 演示给别人看

不适合：

1. 真正让多个朋友同事同时长期在线使用
2. 多人实时协作
3. 公网正式部署

## 6. 功能实现策略

## 6.1 登录注册

本地版建议先做“轻登录”而不是完整线上认证。

建议有两个可选路线：

1. 最省事：单用户模式
2. 稍完整：本地邮箱密码账号

我建议你第一版优先：

`单用户模式`

做法：

1. 默认只有一个当前用户
2. 首次进入时填写昵称、上下班时间
3. 不先做真正的注册和登录流程

原因：

1. 你当前主要是自己用
2. 可以大幅减少认证复杂度
3. 能更快把体验做出来

如果你坚持要保留登录页，也可以做一个非常轻的本地伪登录。

## 6.2 群组

本地版群组建议保留概念，但先按单机模型做。

做法建议：

1. 允许你创建多个群组
2. 群组成员可以先是“本地模拟成员”
3. 群组邀请码先展示但不做真实远程加入

这能保留产品结构，又不会被真实多用户同步卡住。

## 6.3 外卖候选池

外卖候选项按你已经确认的方向，采用：

`长期沉淀`

实现建议：

1. 每个群组拥有自己的长期外卖池
2. 可新增、编辑、停用外卖项
3. 转盘从当前启用项中随机抽选
4. 保留最近转盘结果记录

## 6.4 打卡与工时统计

这部分本地版可以做得比较完整。

建议能力：

1. 上班打卡
2. 下班打卡
3. 每日工时统计
4. 本周累计工时
5. 本月累计工时
6. 相对默认下班时间的加班时长

工时规则建议：

1. 每日工时 = `下班打卡时间 - 上班打卡时间`
2. 如果未打下班卡，当天工时可按“当前时间 - 上班打卡时间”临时计算展示
3. 加班时长 = `实际下班时间 - 默认下班时间`
4. 若提前下班，则加班时长为 0

## 6.5 心情与陪伴角色

这部分是产品灵魂，可以完全保留。

建议：

1. 每天记录一个主心情
2. 首页像素牛马根据心情变化表情
3. 不同心情显示不同小文案

## 7. 推荐数据库模型

本地版即使不用云端，也建议保留一套未来可迁移的数据结构。

推荐核心表：

1. `User`
2. `Group`
3. `GroupMember`
4. `FoodOption`
5. `FoodSpin`
6. `AttendanceRecord`
7. `MoodLog`
8. `Todo`

虽然第一版可以单用户运行，但保留这些结构有两个好处：

1. 页面结构不用以后重做
2. 将来迁到云端时更容易

## 8. Prisma + SQLite 推荐方式

建议目录结构大致如下：

```text
app/
components/
lib/
prisma/
  schema.prisma
  dev.db
```

其中：

1. `schema.prisma` 定义数据模型
2. `dev.db` 是本地 SQLite 数据库文件
3. `lib/` 放 Prisma client 和业务函数

## 9. 首页第一版建议实现范围

首页建议先做这些：

1. 像素牛马 Hero 区
2. 今日心情选择
3. 上下班打卡按钮
4. 距离下班倒计时
5. 今日工时显示
6. 当前群组快捷入口
7. 最近转盘结果

## 10. 群组页第一版建议实现范围

建议先做：

1. 群组名称展示
2. 模拟成员列表
3. 外卖候选池列表
4. 新增外卖项
5. 启用/停用外卖项
6. 转盘抽选
7. 历史转盘结果

## 11. 哪些功能先简化

为了更快做出来，建议先简化这些：

1. 不做真正的第三方登录
2. 不做真正的多用户在线加入
3. 不做实时同步
4. 不做复杂权限
5. 不做消息通知
6. 不做复杂数据统计图

## 12. 后续迁移到云端时怎么办

本地版不是死路，后面可以迁移。

迁移思路通常会是：

1. 保留 Next.js 前端
2. 把 SQLite 换成 Postgres
3. 把 Prisma 数据源从本地文件切到云数据库
4. 再补真正的用户认证和群组加入机制

所以这版的意义是：

`先把产品做出来，再决定数据放到哪里`

## 13. 当前推荐决策

我建议当前正式采用以下决策：

1. 第一版先做本地单机 Web 应用
2. 先采用单用户模式
3. 数据库存储在本地 `SQLite`
4. 使用 `Prisma` 管理数据模型
5. 群组与成员先做本地模拟
6. 打卡与工时统计做完整
7. UI 采用现代 Web + 像素风牛马主视觉

## 14. 下一步最适合做什么

这份方案定下来后，最适合继续的是：

1. 把数据库设计改写成 `Prisma schema`
2. 产出页面接口清单
3. 直接初始化 `Next.js + Tailwind + Prisma + SQLite` 项目骨架
