# 上班陪伴 Web 产品初稿

## 1. 产品概述

### 1.1 产品名称

暂定名：`牛马上班搭子`

### 1.2 一句话描述

一个给上班族和同事小群体使用的陪伴型网页应用，提供上班打卡、下班倒计时、群组一起选外卖、每日心情记录和角色陪伴体验。

### 1.3 产品目标

第一版目标不是做一个完整 OA 或效率平台，而是做一个轻量、有情绪价值、适合同事一起使用的“上班陪伴页”。

核心目标：

1. 让用户上班时愿意打开页面并停留。
2. 帮助小团队快速完成“今天吃什么”的决策。
3. 用打卡、倒计时、心情和角色反馈增强陪伴感。

### 1.4 目标用户

目标用户优先级：

1. 关系较熟的小团队同事
2. 朋友型办公搭子
3. 远程办公或异地协作的小群体

用户特点：

1. 愿意使用轻松、有趣的产品
2. 高频面对“今天吃什么”“还有多久下班”这类日常问题
3. 不需要复杂企业管理能力，更看重轻松氛围和陪伴感

## 2. 产品定位

### 2.1 核心价值

产品提供两类价值：

1. 功能价值：打卡、倒计时、外卖选择、群组协作
2. 情绪价值：牛马角色陪伴、心情表达、群体共鸣

### 2.2 与普通待办或打卡工具的区别

区别不在“是否能记录”，而在“是否值得每天打开”。

本产品的记忆点应放在：

1. 角色化陪伴
2. 群组式午饭决策
3. 与上班情绪强绑定的交互反馈

## 3. MVP 范围

### 3.1 第一版必须包含

1. 注册与登录
2. 创建群组、加入群组
3. 群组内维护外卖候选项
4. 外卖转盘抽选
5. 每日上下班打卡
6. 上下班倒计时
7. 今日心情选择
8. 牛马角色根据心情切换表情或状态

### 3.2 第一版明确不做

1. 企业级审批、请假、考勤管理
2. 复杂权限体系
3. 群聊系统
4. 外卖平台真实下单打通
5. 完整 to do list 协作系统
6. 多角色宠物养成深度玩法

### 3.3 第二阶段可扩展

1. 个人 to do list
2. 群组共享待办
3. 工位陪伴动画
4. 每周情绪报告
5. 午饭偏好推荐
6. 在线状态和同事陪伴面板

## 4. 核心场景

### 4.1 早上上班

用户打开网页：

1. 看见今天的牛马角色状态
2. 选择当前上班心情
3. 点击上班打卡
4. 页面开始显示距离午休或下班的倒计时

### 4.2 中午吃饭

群组成员进入群组页：

1. 查看已有外卖候选项
2. 新增今日想吃的店
3. 启动转盘
4. 得到本次群组推荐结果

### 4.3 下午摸鱼与坚持

用户在工作间隙打开页面：

1. 查看还有多久下班
2. 看到牛马角色反馈当前心情
3. 获得轻度陪伴感和情绪缓冲

### 4.4 下班前

用户点击下班打卡：

1. 记录下班时间
2. 结束今日倒计时
3. 形成当天的出勤记录

## 5. 角色与权限

### 5.1 用户角色

第一版只保留两类角色：

1. 普通用户
2. 群组创建者

### 5.2 权限说明

普通用户可以：

1. 注册登录
2. 创建或加入群组
3. 查看自己打卡记录
4. 选择今日心情
5. 添加外卖候选项
6. 发起或参与转盘

群组创建者额外可以：

1. 编辑群组名称
2. 移除无效或重复的外卖项

第一版不做复杂管理员权限。

## 6. 功能需求

### 6.1 注册与登录

目标：让用户以最低门槛进入产品。

建议能力：

1. 邮箱注册登录
2. 第三方登录可后置
3. 新用户首次登录后创建个人资料

字段建议：

1. 昵称
2. 头像
3. 默认上班时间
4. 默认下班时间
5. 时区

### 6.2 群组

目标：支持同事之间形成共享空间。

功能：

1. 创建群组
2. 通过邀请码加入群组
3. 查看群组成员
4. 查看群组内外卖候选列表

规则建议：

1. 一个用户可加入多个群组
2. 一个群组有一个创建者
3. 邀请码可重复使用，后续再考虑有效期

### 6.3 外卖候选与转盘

目标：快速解决“吃什么”的决策问题。

功能：

1. 群组成员新增外卖候选项
2. 可设置分类，如米饭、面、轻食、咖啡
3. 点击开始转盘
4. 转盘返回随机结果
5. 记录本次抽选结果

第一版规则建议：

1. 所有人都能添加候选项
2. 同名店铺允许先录入，后续再做去重优化
3. 转盘结果只作为推荐，不锁定最终选择

### 6.4 打卡

目标：记录每天上下班时间。

功能：

1. 上班打卡
2. 下班打卡
3. 查看当天状态
4. 查看历史记录

第一版规则建议：

1. 每天最多一条上班记录和一条下班记录
2. 支持当天补打卡一次，是否开放可配置
3. 先不做地理位置校验

### 6.5 倒计时

目标：给用户稳定的时间感知和陪伴感。

功能：

1. 根据用户设置的上下班时间显示倒计时
2. 上班前显示距离上班时间
3. 工作中显示距离下班时间
4. 下班后显示今日已完成

第一版逻辑建议：

1. 倒计时默认基于个人设置时间
2. 如果用户已上班打卡，则倒计时状态切换为“已上班”
3. 如果用户已下班打卡，则停止工作中倒计时

### 6.6 今日心情与牛马角色

目标：提供情绪表达与陪伴体验。

功能：

1. 用户每日选择一个心情
2. 牛马角色根据心情切换视觉状态
3. 可显示一句简短陪伴文案

第一版建议心情：

1. 干劲满满
2. 普通营业
3. 累了
4. 想吃点好的
5. 快下班吧

第一版视觉建议：

1. 每种心情对应一个固定表情
2. 背景统一为“牛马上班中”的办公场景
3. 先做静态插画或简单帧动画，不急着上复杂互动

### 6.7 To Do List

定位：候选扩展功能，不进入 MVP 核心流程。

第二阶段建议：

1. 个人待办
2. 今日完成勾选
3. 与心情和下班倒计时结合

## 7. 页面信息架构

### 7.1 页面列表

1. 登录/注册页
2. 首页
3. 群组页
4. 外卖转盘页
5. 打卡记录页
6. 个人设置页

### 7.2 首页结构

首页应是产品的核心使用页，建议包含：

1. 顶部导航
2. 今日牛马角色展示区
3. 今日心情选择区
4. 上下班状态与倒计时区
5. 快捷打卡按钮
6. 当前群组入口
7. 今日推荐外卖或最近转盘结果

### 7.3 群组页结构

1. 群组名称和邀请码
2. 成员列表
3. 外卖候选列表
4. 新增外卖项表单
5. 开始转盘按钮
6. 历史抽选结果

### 7.4 打卡记录页结构

1. 今日打卡状态
2. 本周打卡记录
3. 历史记录筛选

### 7.5 设置页结构

1. 个人资料
2. 默认上班时间
3. 默认下班时间
4. 默认群组
5. 提醒与通知预留位

## 8. 关键交互与体验要求

### 8.1 产品气质

产品整体气质应是：

1. 轻松
2. 有点幽默
3. 可爱但不幼稚
4. 陪伴感强

### 8.2 视觉方向

建议：

1. 主视觉围绕“牛马上班工位”
2. 角色表情随心情变化
3. 页面背景和角色状态有日内节奏感
4. 核心数据模块保持清晰，不要过度装饰影响可读性

### 8.3 关键交互

1. 心情选择后角色即时变化
2. 打卡后倒计时区即时更新
3. 转盘动画有明确仪式感
4. 群组结果展示有轻分享感

## 9. 非功能需求

### 9.1 性能

1. 首页首屏加载尽量控制在 3 秒内
2. 核心数据区刷新应足够流畅

### 9.2 兼容性

1. 优先支持桌面端网页
2. 同时兼顾手机端基础可用

### 9.3 安全

1. 用户数据需与群组权限隔离
2. 打卡记录、心情记录、群组数据只能在授权范围内访问

## 10. 推荐技术方案

## 10.1 技术栈建议

前端：

1. `Next.js`
2. `TypeScript`
3. `Tailwind CSS`
4. `shadcn/ui`

后端与数据：

1. `Supabase Auth`
2. `Supabase Postgres`
3. `Supabase Realtime`
4. `Supabase Storage`

部署：

1. `Vercel`
2. `Supabase`

### 10.2 推荐原因

这套方案适合当前阶段，因为：

1. 登录、数据库、实时能力可以较快搭起来
2. 对小团队 MVP 成本较低
3. 后面扩展群组、实时在线状态、简单通知也比较顺

## 11. 数据库表设计草案

### 11.1 profiles

用途：用户资料表

字段建议：

1. `id` UUID，主键，对应认证用户 ID
2. `nickname` varchar
3. `avatar_url` text
4. `work_start_time` time
5. `work_end_time` time
6. `timezone` varchar
7. `created_at` timestamptz
8. `updated_at` timestamptz

### 11.2 groups

用途：群组表

字段建议：

1. `id` UUID，主键
2. `name` varchar
3. `invite_code` varchar，唯一
4. `owner_id` UUID，关联 `profiles.id`
5. `created_at` timestamptz
6. `updated_at` timestamptz

### 11.3 group_members

用途：群组成员关系表

字段建议：

1. `id` UUID，主键
2. `group_id` UUID，关联 `groups.id`
3. `user_id` UUID，关联 `profiles.id`
4. `role` varchar，默认 `member`
5. `joined_at` timestamptz

约束建议：

1. `group_id + user_id` 唯一

### 11.4 food_options

用途：群组外卖候选项

字段建议：

1. `id` UUID，主键
2. `group_id` UUID，关联 `groups.id`
3. `name` varchar
4. `category` varchar，可空
5. `created_by` UUID，关联 `profiles.id`
6. `is_active` boolean，默认 true
7. `created_at` timestamptz

### 11.5 food_spins

用途：每次转盘记录

字段建议：

1. `id` UUID，主键
2. `group_id` UUID，关联 `groups.id`
3. `selected_food_option_id` UUID，关联 `food_options.id`
4. `started_by` UUID，关联 `profiles.id`
5. `created_at` timestamptz

### 11.6 attendance_records

用途：上下班打卡记录

字段建议：

1. `id` UUID，主键
2. `user_id` UUID，关联 `profiles.id`
3. `work_date` date
4. `clock_in_at` timestamptz，可空
5. `clock_out_at` timestamptz，可空
6. `status` varchar，可选值如 `working` `finished` `missed`
7. `created_at` timestamptz
8. `updated_at` timestamptz

约束建议：

1. `user_id + work_date` 唯一

### 11.7 mood_logs

用途：每日心情记录

字段建议：

1. `id` UUID，主键
2. `user_id` UUID，关联 `profiles.id`
3. `work_date` date
4. `mood_code` varchar
5. `created_at` timestamptz

约束建议：

1. `user_id + work_date` 唯一

### 11.8 todos

用途：第二阶段个人待办

字段建议：

1. `id` UUID，主键
2. `user_id` UUID，关联 `profiles.id`
3. `title` varchar
4. `is_done` boolean
5. `due_date` date，可空
6. `created_at` timestamptz
7. `updated_at` timestamptz

## 12. 核心业务规则建议

1. 同一个用户每天只能有一条打卡记录。
2. 同一个用户每天只能选择一次主心情。
3. 一个用户可以加入多个群组。
4. 一个群组可以有多个外卖候选项。
5. 转盘结果只记录“被选中项”和“发起人”，不需要保留复杂过程数据。
6. 第一版先不做复杂审核和审批。

## 13. 开发优先级建议

### Phase 1：能跑起来

1. 登录注册
2. 用户资料
3. 首页
4. 打卡与倒计时
5. 今日心情

### Phase 2：形成群组玩法

1. 创建群组
2. 加入群组
3. 外卖候选列表
4. 转盘结果记录

### Phase 3：增强陪伴感

1. 牛马角色状态优化
2. 更细的情绪反馈
3. 历史记录和统计

## 14. 下一步待产出

这个文档确认后，建议紧接着产出：

1. 页面线框图
2. 数据库 SQL 建表草案
3. 接口清单
4. MVP 开发排期
