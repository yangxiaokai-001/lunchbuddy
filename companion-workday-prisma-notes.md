# Prisma Schema 说明

## 1. 当前设计目标

这份 `Prisma schema` 是按下面这个前提写的：

1. 第一版先跑在你自己电脑上
2. 数据库存储使用 `SQLite`
3. 产品先按单用户 MVP 做
4. 数据结构尽量保留未来迁移到云端数据库的可能

对应文件：

[schema.prisma](/Users/katey.yang/Documents/Codex/2026-05-19/npx-ccusage-daily-since-2026-05/prisma/schema.prisma)

## 2. 为什么即使本地版也保留多用户结构

虽然第一版主要是你自己用，但我还是保留了：

1. `User`
2. `Group`
3. `GroupMember`

这样做的好处是：

1. 页面和产品结构不需要以后重做
2. 未来迁移到线上时更顺
3. 现在就可以在本地模拟“同事群组”

## 3. 几个关键字段说明

### 3.1 User

`email` 和 `passwordHash` 目前允许为空。

原因：

1. 如果你第一版走单用户模式，这两个字段可以先不用
2. 如果后面想补本地邮箱登录，也不用大改表结构

### 3.2 workStartTime / workEndTime

这里我先用 `String` 存 `"09:00"` 这种格式，而不是直接用数据库 `time` 类型。

原因：

1. SQLite 对时间类型支持比较松
2. 字符串在本地版处理更简单
3. 前端和后端都更容易直接读写

### 3.3 AttendanceRecord.workDate

这里先用 `DateTime`。

实际使用时建议存成“当天零点”的日期时间，作为该天唯一键的一部分。

这样可以保证：

1. 一个用户一天只会有一条记录
2. 后面做周统计和月统计也比较顺

### 3.4 workMinutes / overtimeMins

我把工时统计结果直接落字段了。

这样做的好处：

1. 历史数据展示简单
2. 周月累计更方便
3. 后面做统计页时不必每次全量重算

推荐规则：

1. 上班后、未下班前：页面临时计算当前工时
2. 下班打卡时：把最终 `workMinutes` 和 `overtimeMins` 写入数据库

## 4. MoodCode 设计

目前保留了这几个枚举值：

1. `ENERGETIC`
2. `NORMAL`
3. `TIRED`
4. `HUNGRY`
5. `WAITING_FOR_OFF_WORK`

这样前端做像素牛马表情切换会比较清楚。

## 5. 群组和外卖池设计

当前设计已经支持：

1. 一个用户拥有多个群组
2. 一个群组包含多个成员
3. 一个群组维护长期外卖池
4. 转盘结果保留历史记录

`FoodOption.isActive` 是给“长期沉淀但可停用”准备的。

这很适合你的外卖池设定。

## 6. 这版暂时没做的东西

这份 schema 还没有加入：

1. 图片上传表
2. 通知提醒
3. 真正的会话登录
4. 复杂操作日志

原因很简单：第一版先把主流程跑通。

## 7. 下一步最自然的动作

这份 schema 定下来后，建议马上接：

1. `seed` 一批本地测试数据
2. 定义首页所需的数据读取函数
3. 定义打卡、心情、转盘的 API
4. 初始化 `Next.js + Prisma + SQLite` 项目骨架
